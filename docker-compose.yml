services:
  app:
    image: sillvva/mattdekok.dev:latest
    container_name: app
    build: .
    environment:
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000
    ports:
      - "80:3000"
    volumes:
      - .:/usr/mattdekok/app
      - /usr/mattdekok/app/frontend/node_modules