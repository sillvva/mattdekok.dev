{
  "name": "mattdekok.dev",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cd frontend && npm run generate && cd ../functions && npm run serve",
    "dev": "cd frontend && npm run dev"
  }
}